<div class="container px-5 pt-3">

    <%= alerts %>
    
    <%= form_for @user, url: complete_signup_path(@user) do |f| %>
        <div class="form-row" >
            <div class="card col-md-12 mb-3 bg-light">
                <div class="card-body">
                    <h4 class="text-center">Add username for <%= @user.email %> to complete sign up</h4>
                </div>
            </div>
            <div class="col-md-12 mb-3">
                <%= f.label :username %>
                <%= f.text_field :username, class: form_class(@user, :username) %>
                <%= invalid_feedback(@user, :username) %>
            </div>
        </div>
        <% unless @user.admin || @user.admin_request %>
            <div class="form-group">
                <%= f.check_box :admin_requested %>
                <%= f.label :admin_requested, "Request admin rights" %>
            </div>
        <% end %>
        <%= f.submit "Complete sign up", class: "btn btn-outline-primary btn-block" %>
    <% end %>
</div>